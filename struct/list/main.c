#include <stdio.h>
#include "dl-list.h"
int main()
{
	struct DL_List * dl_list = create_DL_List();

	//add_to_DL_List(dl_list, &"hello");
	add_to_DL_List(dl_list, &i);
	//add_to_DL_List(dl_list, &1);
	//add_to_DL_List(dl_list, &0);
	
	printf("%d", n2);

	return 0;
}

